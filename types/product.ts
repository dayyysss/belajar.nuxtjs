export interface Products {
    id?: number,
    name?: string,
    category?: string,
    price?: number,
    image?: string,
    color?: string,
    isCart?: boolean,
    description?: string,
    quantity?: number,
    }